// lib/pages/broadcast_page.dart

import 'package:flutter/material.dart';

class BroadcastPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Broadcast'),
      ),
      body: Center(
        child: Text('Broadcast Page Content'),
      ),
    );
  }
}
